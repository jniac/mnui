export const vectorCss = /* css */`

#mnui .vector .vector-property + .vector-property {
  margin-left: 4px;
}

#mnui .vector .vector-property.hovered,
#mnui .vector .vector-property.focused {
  --color: var(--active-color);
  color: var(--color);
}

`
